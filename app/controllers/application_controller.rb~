class ApplicationController < ActionController::Base
  protect_from_forgery
  
  before_filter :find__current_user
  
  def find_current_user
    @current_user ||= User.find_by_id(session[:user_id])
  end
  
  def current_user
    @current_user
  end
    
end
