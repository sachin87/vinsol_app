<%- rel= current_user.relationships.find_by_followed_id(@user) %>
<%= form_for(rel, url: relationship_path(rel,:format => :js), html: {method: :delete, id: "delete-form"}) do |f| %>
<% f.submit "Unfollow", class: "btn btn-large" %>
<% end %>

